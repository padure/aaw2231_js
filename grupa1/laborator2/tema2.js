// Instructiunile conditionale
// If
if (condition) {
    //codul pentru adevarat
}

// If-else
if (condition) {
    //codul pentru adevarat
} else {
    //codul pentru fals
}
// switch
let key = value;
switch (key) {
    case value:
        
        break;

    default:
        break;
}
// Exemplu 1
let userRole = 'admin';
switch (key) {
    case 'guest':
        console.log('Guest user');
        break;
    case 'admin':
        console.log('Admin user');
        break;
    default:
        console.log('Unknown user role');
        break;
}